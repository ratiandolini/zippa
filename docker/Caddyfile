{$DOMAIN} {
	encode zstd gzip
	tls {$TLS_EMAIL}

	@static path /_next/static/* /photos/* /favicon.ico /manifest.json
	header @static Cache-Control "public, max-age=31536000, immutable"

	reverse_proxy app:3000 {
		health_uri /api/health
		health_interval 30s
	}
}

# www → apex
www.{$DOMAIN} {
	redir https://{$DOMAIN}{uri} permanent
}
